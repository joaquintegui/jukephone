name: Deploy to Pi

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, jukephone]
    steps:
      - name: Pull latest code
        run: |
          cd ~/jukephone
          git pull origin main

      - name: Restart service
        run: sudo systemctl restart jukephone
